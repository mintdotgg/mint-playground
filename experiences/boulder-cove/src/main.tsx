import React from "react";
import ReactDOM from "react-dom/client";
import MapleCove from "./MapleCove";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MapleCove />
  </React.StrictMode>,
);
